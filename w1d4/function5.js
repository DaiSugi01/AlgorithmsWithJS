// Function 5: return the area of a triangle when given two parameters, 
// a Base width, and a height. 

const calculateTriangleArea = (width, height) => {
  return width * height / 2;
}

// Should be 100
console.log(calculateTriangleArea(10, 20));